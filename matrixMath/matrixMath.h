#pragma once

#include "matrix.h"

namespace MatrixMath {
void add(Matrix, Matrix);
void sub(Matrix, Matrix);
Matrix mul(Matrix, Matrix);
}

// class MatrixMath {
// 	public:
// 		void add(Matrix, Matrix);
// 		void sub(Matrix, Matrix);
// 		Matrix mul(Matrix, Matrix);
// };